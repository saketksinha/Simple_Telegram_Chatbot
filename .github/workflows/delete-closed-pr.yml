on:
  pull_request:
    types: [closed]
jobs:
 delete-branch:
   runs-on: ubuntu-latest
   steps:
    - name: delete branch
      uses: actions/delete-merged-branch@main
      env:
       GITHUB_TOKEN: ${{ secrets.SECRET_TOKEN }}
       delete_closed_pr: true
