name: delete branch on close pr
on: 
  pull_request:
    types: [closed]
  
jobs:
  delete-branch:
    runs-on: ubuntu-latest
    steps:
      - name: delete branch
        uses: ./.github/workflows/delete-merged-branch@main
        env:
         GITHUB_TOKEN: ${{ secrets.SECRET_TOKEN }}
