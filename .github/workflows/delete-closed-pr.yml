name: delete branch on close pr
on:
  pull_request:
    types: [closed]
jobs:
  delete-branch:
    runs-on: ubuntu-latest
    steps:
      - name: delete branch
        uses: actions/delete-closed-pr@main
        env:
          GITHUB_TOKEN: ${{ secrets.SECRET_TOKEN }}
          delete_closed_pr: true
